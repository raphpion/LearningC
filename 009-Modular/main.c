#include <stdio.h>
#include <stdlib.h>

/* LES PROTOTYPES
- permettent de placer une fonction après la fonction main.

type nomFonction(paramètre1, paramètre2);

LES HEADERS

#include "fichier.h"
#include "fichier.c"

static avant une variable la rend uniquement accessible dans ce fichier
static avant une variable dans une fonction la rend globalement accessible
static avant une fonction la rend locale à ce fichier

*/

int main()
{
  return 0;
}